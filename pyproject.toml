[project]
name = "lists"
version = "0.1.0"
description = "Lists for TODO's Tasks etc etc!"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "jinja2>=3.1.6",
    "pydantic>=2.11.4",
    "python-multipart>=0.0.20",
    "typing>=3.10.0.0",
    "uvicorn>=0.34.2",
]
